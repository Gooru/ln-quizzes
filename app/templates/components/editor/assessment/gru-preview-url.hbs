<span>{{t 'common.preview'}}</span>
<div class="preview {{if url 'show-url' 'show-legend'}}">
    {{#if url}}
        <iframe src={{url}} style={{resourceHeight}}></iframe>
      {{else}}
        <span>{{t 'gru-rubric-creation.gru-preview-url.add'}}</span>
    {{/if}}
</div>
